{% extends "_layouts/layout.twig" %}

{% set posts = craft.entries.section('blogPosts').relatedTo(category).all() %}

{% block content %}

	<h1>
		Blog Posts in “{{ category.title }}”
	</h1>

	{% include "_includes/listing" with { posts: posts } only %}
{% endblock %}
